{% extends 'base.html.twig' %}

{% block title %}Aeroport index{% endblock %}

{% block body %}
    <h1>Aeroport index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Nom_ae</th>
                <th>Pays</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for aeroport in aeroports %}
            <tr>
                <td>{{ aeroport.id }}</td>
                <td>{{ aeroport.nomAe }}</td>
                <td>{{ aeroport.pays }}</td>
                <td>
                    <a href="{{ path('aeroport_show', {'id': aeroport.id}) }}">show</a>
                    <a href="{{ path('aeroport_edit', {'id': aeroport.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('aeroport_new') }}">Create new</a>
{% endblock %}
