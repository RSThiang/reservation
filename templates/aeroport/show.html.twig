{% extends 'base.html.twig' %}

{% block title %}Aeroport{% endblock %}

{% block body %}
    <h1>Aeroport</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ aeroport.id }}</td>
            </tr>
            <tr>
                <th>Nom_ae</th>
                <td>{{ aeroport.nomAe }}</td>
            </tr>
            <tr>
                <th>Pays</th>
                <td>{{ aeroport.pays }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('aeroport_index') }}">back to list</a>

    <a href="{{ path('aeroport_edit', {'id': aeroport.id}) }}">edit</a>

    {{ include('aeroport/_delete_form.html.twig') }}
{% endblock %}
